
= form_for @locations_region, :html => { "data-remote" => "true" }  do |f|
  - if @locations_region.errors.any?
    #error_explanation 
      %h2= "No se pudo guardar:"
      %ul
        - @locations_region.errors.full_messages.each do |msg|
          %li= msg
  
    
  .field
    = f.label :name, 'Nombre' 
    = f.text_field :name, { class: "required ", "maxlength" => "60", "minlength" => "2"  }
    
  .field
    = f.label :country_id 
    = f.text_field :country_id, { class: "required ", "maxlength" => "60", "minlength" => "2"  }
    .actions
    = f.submit 'Guardar', { data: { icon: 'plus', theme: 'b'} }
