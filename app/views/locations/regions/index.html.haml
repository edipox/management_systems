
= will_paginate @locations_regions
%ul{ data: { role: 'listview', inset: 'true', theme: 'c', filter: 'true' } }
  %li{ data: { role: 'list-divider' } }
  
    %div{ class: "ui-grid-b" }
  
      
      %div{ class: "ui-block-b" } Nombre 
      
      %div{ class: "ui-block-c" }  
  
  - @locations_regions.each do |locations_region|
    %li{ class: 'element'}
      %div{ class: "ui-grid-b" }
      
        %div{ class: "ui-block-b trim" }= locations_region.name
      
        %div{ class: "ui-block-c trim" }= locations_region.country_id
  
        %div{  class: "ui-block-d", data: { role: 'controlgroup', type: 'horizontal' } }
          -# if can? :delete, locations_region 
          %a{ href: locations_region_path(locations_region), data: { method: :delete, confirm: 'Esta seguro?', role: 'button', icon: 'delete', mini: 'true', remote: 'true' } } Eliminar
          -# if can? :update, locations_region 
          %a{ href: edit_locations_region_path(locations_region), data: { role: 'button', icon: 'grid', mini: 'true', remote: 'true' } } Editar

  = link_to 'Agregar  ', new_locations_region_path, { style: 'width: 250px; float: right', data: { role: 'button', theme: 'b', icon: 'new', remote: 'true'  } }
