
= form_for @components_categories_kind, :html => { "data-remote" => "true" }  do |f|
  - if @components_categories_kind.errors.any?
    #error_explanation 
      %h2= "No se pudo guardar:"
      %ul
        - @components_categories_kind.errors.full_messages.each do |msg|
          %li= msg
  
    
  .field
    = f.label :name, 'Nombre' 
    = f.text_field :name, { class: "required ", "maxlength" => "60", "minlength" => "2"  }
  %div.ui-grid-a
    %div.ui-block-a
      %a.cancel{ data: {role:'button', icon: 'delete', theme: 'a' } } Cancelar
    %div.ui-block-b  
      %input{ close: "#componentPopup", type: 'submit', value: 'Guardar', data: { icon: 'plus', theme: 'b' } }
